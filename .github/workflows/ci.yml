name: Demo

on:
  push:
    branches:
    - master
    paths:
    - '.github/workflows/**'
    - 'hello.txt'
  pull_request:
    branches:
      - master

jobs:
  hello:
    name: Hello World
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Greeting
      run: |
        cat hello.txt
        echo github.ref = ${{ github.ref }}
        echo github.sha = ${{ github.sha }}

  docker:
    name: Docker
    runs-on: ubuntu-latest
    steps:
    - name: Docker
      run: docker --version
